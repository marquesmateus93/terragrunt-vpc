name: Terragrunt VPC
on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  TerraformInstall:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        shell: bash

      - name: Install and setup version.
        uses: ./
        with:
          version: 1.3.6
        shell: bash